 // for of  loop 

 // ["", "" , ""]
 // [{} ,{}, {}]
  
 const arr = [ 1 ,2,3,4,5]
 for ( const iterator of arr ) {
    // we can take anythings in place of iterator such as i , j , gaganva anythings
    console.log(iterator);
 }
  


  const greet = "babu ho jara me"
  for (const i of greet){
    console.log( ` Each char is ${greet }`)

  }




  // maps is an object that holds keyvalue in pair  and remembers the original
  // insertion order of the keys . Any value ( both objects and primitive values ) may be used as either a key or a value . 
  // it has unique vlaues and the occurance of value is only once  in this iteration happens in the same order in which the keyvalue pair wa inserted into map 
      const map  = new Map()
      map.set ( 'In ' , "india")
      map.set ( ' USA' , "united States of America ")
      map.set ( ' Fr' , "France")
      console.log (map);
    
  // lovely output 

      /*Map(3) {
  'In ' => 'india',
  ' USA' => 'united States of America ',
  ' Fr' => 'France'
}*/

// implementing the loops over map
 for (const [key, value ] of map) {
     console .log (key , ':-' , value);

 }
 const  myObject = {
    'game1' : 'yes',
    'game2' : "yes yes"

 }
 for ( cost [key ,value] of myObject){
    console.log (key , ":-" ,value);

 }
//TypeError: myObject is not iterable